git rm -f -r "builds"
git rm -f -r "devel"
git rm -f -r "docs"
git rm -f -r "objs"
git rm -f -r "subprojects"
git rm -f -r "tests"
git rm -f ".clang-format"
git rm -f ".gitignore"
git rm -f ".gitlab-ci.yml"
git rm -f ".gitmodules"
git rm -f ".mailmap"
git rm -f "autogen.sh"
git rm -f "CMakeLists.txt"
git rm -f "configure"
git rm -f "Makefile"
git rm -f "meson.build"
git rm -f "meson_options.txt"
git rm -f "modules.cfg"
git rm -f "README.git"
git rm -f "vms_make.com"

git mv "./src" "./source"
git mv "./README" "./ReadMe.md"
git mv "./LICENSE.TXT" "License"

